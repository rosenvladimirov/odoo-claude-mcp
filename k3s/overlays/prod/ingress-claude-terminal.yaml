apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: claude-terminal
spec:
  routes:
    - match: Host(`terminal.example.com`)   # <-- EDIT
      kind: Rule
      services:
        - name: claude-terminal
          port: 8080
