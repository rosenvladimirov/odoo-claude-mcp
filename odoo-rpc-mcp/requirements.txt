mcp>=1.0.0
uvicorn>=0.30.0
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0
telethon>=1.36.0
paramiko>=3.4.0
httpx>=0.27.0
markdown>=3.5.0
requests>=2.31.0
lxml>=5.2.0
argon2-cffi>=23.1.0
itsdangerous>=2.2.0
# Optional — PDF sanitization in ai_vision_service (Gap 4.3). When
# present the vision extractor strips /JS, /OpenAction, /EmbeddedFiles
# before sending the PDF to Claude. If missing the sanitizer falls
# back to returning the original bytes + logs a warning.
pypdf>=4.0.0
# 2.24.0: admin backup manager needs boto3 (Contabo S3 via eu2.contabostorage.com).
boto3>=1.34.0
# 2.24.0: Prometheus /metrics scaffold. Full scraping stack is 3.x.
prometheus-client>=0.20.0
# 2.24.0: optional — APScheduler drives daily backup auto-rotation when present.
# Missing → manual rotation only (endpoint + admin UI button still work).
APScheduler>=3.10.0
# 2.25.0: pyzipper — AES-encrypted ZIP write/read. Used by mcp_terminal_get_config
# tool to package config keys with a temp password for the Odoo Setup Wizard.
# stdlib zipfile can only READ encrypted zips, not WRITE. pyzipper extends it.
pyzipper>=0.3.6
