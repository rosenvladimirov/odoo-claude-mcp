# Зависимости само за тестовете — НЕ влизат в образа.
# Държим ги отделно, за да е едно и също локално и в CI: всяко нещо, което
# тестът вика през pytest.importorskip, трябва да е тук, иначе тестът тихо
# се прескача и зеленото не значи нищо.
pytest>=8.0.0

# test_centrifugo_template.py прави importorskip("yaml"). Runtime кодът НЕ
# ползва yaml — само тестът чете client_stack.template.yml.
PyYAML>=6.0
